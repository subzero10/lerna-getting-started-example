import React from 'react';

export function Header() {
    return (
        <header>
            Lerna is the original monorepo tool!
            (simulating a fix to a dependency package)
        </header>
    );
}
